```mermaid
flowchart TD;
  id1[User sees list of notes] --> id2[They enters their own text(note) in the input box];
  id2 --> id3[Presses save button];
  id3 --> id4[Text(note) appears in the bottom of the list];
  id4 --> id5[User refreshes the page];
  id5 --> id6[Note is still visible in the list];
```
