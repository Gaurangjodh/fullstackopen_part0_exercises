```mermaid
flowchart TD;
  User sees list of notes --> They enters their own text(note) in the input box;
  They enters their own text(note) in the input box --> Presses save button;
  Presses save button --> Text(note) appears in the bottom of the list;
  Text(note) appears in the bottom of the list --> User refreshes the page;
  User refreshes the page --> Note is still visible in the list;
```
