```mermaid
flowchart TD;
  id1[User sees list of notes] --> id2[They enters their own text(note) in the input box];
  id2[They enters their own text(note) in the input box] --> id3[Presses save button];
  id3[Presses save button] --> id4[Text(note) appears in the bottom of the list];
  id4[Text(note) appears in the bottom of the list] --> id5[User refreshes the page];
  id5[User refreshes the page] --> id6[Note is still visible in the list];
```
